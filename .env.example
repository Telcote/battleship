# Шаблон окружения для docker-compose. Скопировать в .env и подставить свои значения.
# Хост `db` — контейнер Postgres из .devcontainer, доступный по сети proxynet.
DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/battleship
ARENA_URL=http://arena:8080
